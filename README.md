# terraform-aws-docker-swarm-app
This repository contains Terraform configurations to set up a Docker Swarm cluster on AWS, which deploys a sample application. The infrastructure includes a VPC, subnets, security groups, an Application Load Balancer (ALB), Auto Scaling Groups (ASG), and EC2 instances.
